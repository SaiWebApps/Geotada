"""New-city onboarding core: typed source/job contracts, an in-memory job store,
and the ingest fetch allowlist (the grey-zone network wall). Purely additive —
nothing existing imports this package."""
