# ====================
# Python
# ====================

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
!mobile/lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
pytest_cache/

# Translations
*.mo
*.pot

# Sphinx documentation
docs/_build/

# pyenv
.python-version

# Celery
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Virtual environments
.env
.venv/
env/
venv/
ENV/
env.bak/
venv.bak/
.virtualenv/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# ruff
.ruff_cache/

# ====================
# IDEs and Editors
# ====================

# VSCode
.vscode/
*.code-workspace

# PyCharm / JetBrains
.idea/
*.iml
*.ipr
*.iws
out/

# Sublime Text
*.sublime-project
*.sublime-workspace

# Vim
*.swp
*.swo
*.swn
*~
.vim/

# Emacs
*~
\#*\#
/.emacs.desktop
/.emacs.desktop.lock
*.elc
auto-save-list
tramp
.\#*

# ====================
# Operating Systems
# ====================

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
*.stackdump
[Dd]esktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msix
*.msm
*.msp
*.lnk

# Linux
*~
.fuse_hidden*
.directory
.Trash-*
.nfs*

# ====================
# Docker
# ====================
.docker/
docker-compose.override.yml
*.log

# ====================
# Database
# ====================
*.db
*.sqlite
*.sqlite3

# ====================
# Logs
# ====================
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ====================
# Frontend / Node.js
# ====================
node_modules/
npm-debug.log
yarn-error.log
.npm/
.yarn/
.pnp.*

# ====================
# Secrets and Credentials
# ====================
*.pem
*.key
*.crt
*.cer
secrets/
.secrets/
credentials.json
service-account*.json

# ====================
# Misc
# ====================
*.bak
*.tmp
*.temp
*.orig
.cache/
tmp/
temp/

# ====================
# Project Specific
# ====================

# Claude Code working state (keep shared commands, learnings, and the Judge
# Protocol infrastructure — agents, guard hook, hook wiring; ignore the rest)
.claude/*
!.claude/commands/
!.claude/LEARNINGS.md
!.claude/agents/
!.claude/ledger/
!.claude/hooks/
!.claude/workflows/
!.claude/settings.json
!.claude/team-engine.js
!.claude/team-engine.test.js
.claude/hooks/guard-log.txt
.claude/hooks/team-gate-log.txt
# The tracker's database. Binary, rewritten on every agent write, and its
# WAL/shm siblings churn constantly. The SCHEMA is version-controlled in
# .claude/ledger/track.py; the rows are this machine's run, not history.
.claude/ledger/tracker.db
.claude/ledger/tracker.db-wal
.claude/ledger/tracker.db-shm

# Keep .env.example and .env.test.example but ignore .env and .env.test
!.env.example
.env.test
!.env.test.example
.env.local
.env.cloud
# Generated audio files (local TTS output)
audio_store/

# Beat dedup review reports — intermediate human-review artifacts.
# Keep _log.jsonl tracked as audit trail; exclude per-run markdown reports.
data/*/_dedup_review/*.md
!data/*/_dedup_review/_log.jsonl

# Source-text PDFs under Books/ are large copyrighted third-party
# materials (50+ MB each, 200+ MB total). The chunked plain-text
# extracts under Books/{city}/{book-slug}/ are the canonical
# pipeline input and remain tracked.
Books/**/*.pdf

# Playwright test screenshots and reports
tests/reports/

# Local sandbox copy of Paris pipeline data — not referenced by tests or scripts.
data/paris_test/

# uv.lock is committed (reproducible dependency resolution)
# Do NOT add uv.lock to this file.

# Valhalla OSM extracts + built tiles (multi-GB, machine-local; make valhalla-build-tiles)
valhalla/custom_files/
.claude/hooks/guard-log.txt

# Scratch tour artifacts (make tour-build / measure-governor output) — machine-local
# reproduction output, never committed. Keeping them ignored removes the recurring
# "git add -A sweeps the scratch corpus into a commit" trap.
data/*/tours/

# Paid evaluator receipts are local audit/runtime state, never source or fixtures.
# Keep the authored tour batch (`tour-batch-v1`) visible for regression tests.
data/certification/tour-batch-review-v*/

# One-off paid text-candidate runs are local runtime/audit state. The checked
# regression corpus lives in `data/certification/tour-batch-v1` instead.
data/certification/*/text-candidate-*/

# Paid authoring reservation markers are resumability state, not regression fixtures.
data/certification/tour-batch-v*/**/attempt-*.json

# Superseded copies of a certification batch. Kept 'just in case' after a re-run,
# read by nothing, and indistinguishable from the live corpus by filename — the
# only thing that separates them is this rule.
data/certification/*-superseded-*/

# Vendored third-party skill bundles dropped in by tooling. Not this project's
# code, carry their own licences, and 146 files would otherwise be swept in by
# any `git add -A`.
.agents/

# /team and /spec-pm working folders are scratch: plan, land the change, delete the
# folder (CLAUDE.md §7). Already-tracked reference specs stay tracked — this only
# stops NEW ones being committed. A genuine permanent reference goes in with `git add -f`.
specs/*/
!specs/_templates/

# The spec folders below are NOT scratch: shipping code points at a file in each
# one — calibration sets the rubric loads, pipeline state files, fixtures three
# scripts read at runtime, and the design documents cited from src/tour/. They
# were tracked while the rule above called them junk, which made every edit of
# them look like a violation. Un-excluding the DIRECTORY is what re-admits its
# contents; a file-level exception cannot reach inside an excluded directory.
#
# Five were added 2026-08-29 after a sweep of 543 staged deletions was checked
# against the product surface before it was committed. Three of them held files
# that code OPENS, and all three folders were already gone from disk:
#
#   2026-07-21-tour-certification    scripts/tour_text_candidate.py:51 defines
#                                    SPEC as this folder and reads
#                                    live-route-summary.json (:109) and
#                                    live-tour-request.json (:111);
#                                    scripts/tour_batch_review.py:61 points here too
#   2026-07-22-tour-batch-regression scripts/tour_batch_candidate.py:49-50 reads
#                                    requests.v1.json from it
#   2026-07-02-dwell-audio-reconciliation
#                                    src/tour/selection.py:515 and
#                                    scripts/measure_planned_audio.py:4 cite its
#                                    design documents for decisions they implement
#   2026-06-14-compose-narrator      src/tour/authoring.py:150 cites it as the
#                                    source of the LOCKED narrator voice
#   2026-07-13-compose-correct-dont-reject
#                                    src/tour/contract.py:1195 cites it
#
# That is the same defect as the unanchored `models/` above, a second time: a
# rule that calls product-read files junk. production-junk-guard.py:255-260 says
# it plainly — any block naming source is this defect showing itself again.
#
# This does not open the folders up. Nothing inside them is named by the keep
# list or sits in a surface directory, so the junk guard's second arm still
# refuses any new file here that the product does not read.
!specs/2026-04-23-vallois-reextract/
!specs/2026-06-13-tour-planner-canonical/
!specs/2026-06-14-compose-narrator/
!specs/2026-07-02-dwell-audio-reconciliation/
!specs/2026-07-12-new-city-onboarding/
!specs/2026-07-13-compose-correct-dont-reject/
!specs/2026-07-16-tour-craft/
!specs/2026-07-18-tour-qa-campaign/
!specs/2026-07-19-tour-quality-standard/
!specs/2026-07-21-tour-certification/
!specs/2026-07-22-tour-batch-regression/
!specs/2026-08-03-premium-tour-crash-never-silent/
!specs/2026-08-07-tour-algorithm-redesign/
!specs/2026-08-29-change-gates/

# graphify's codebase map: 50MB of generated chunks, regenerated by the graphify
# skill. graph.json and GRAPH_REPORT.md are read by tooling from the working
# copy; nothing here belongs in history.
graphify-out/

# CodeGraph's SQLite index of this workspace, plus the self-ignoring stub it
# writes inside itself. Regenerated by the indexer from the working copy; the
# product does not read it and nothing that builds, tests or ships names it.
.codegraph/

# Local TTS model weights (fetched by preflight; never committed — 337MB).
# Anchored: an unanchored `models/` also silently excluded src/api/models/ and
# mobile/lib/models/ — real product source, tracked only because it predated
# this rule and would have been lost on any fresh add.
/models/

# The city pipeline's own run state and audit reports. The pipeline marks these
# with a leading dot — .pipeline-state, .pipeline-results, .area-classify,
# .batch-results, .gravity-*, .poi_research_batch_*, .upload-report-* — to keep
# its scratch separate from the corpus sitting beside it. 106 of them ended up
# tracked anyway. They are resume markers and one-off reports, never input: a
# fresh clone runs the pipeline identically without them.
data/*/.*
