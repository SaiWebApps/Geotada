{
  "topic": "tour-engine-convergence",
  "created": "2026-07-26",
  "tier": 2,
  "approved_by_human": true,
  "approved_at": "2026-07-27T04:34:26Z",

  "baseline": {
    "lint": "make lint -> All checks passed! (2026-07-26)",
    "commit": "930b1e2 (dirty tree: 31 modified files + 4 untracked specs dirs)",
    "note": "make _test-golden is RED at clean HEAD (both cases, 0.0% overlap) for an unrelated fixture-provenance defect: fixtures/tour_golden/*.json pin UUID beat ids, 0 of 43 of which exist in data/paris/beats.json (whose ids are slugs). OUT OF SCOPE here, and never re-baselined inside this work. Every phase gate below cites _test-python, never _test-golden."
  },

  "decisions": {
    "D1-anthropic-client": "src/tour/anthropic_client.py is a shared SDK helper, not tour logic (imported by src/api/routes/feedback.py and src/onboard/beat_draft.py). It stays inside the firewall's FROZEN_EXCEPTIONS ratchet for now rather than being moved to src/llm/ — moving it is a rename touching 11 importers and would inflate this change. Revisit when the exception set is drained.",
    "D2-goldens": "The RED golden fixtures are a separate ledger. They are the only artifact that could prove selection behaviour did not change, so repairing them inside this change would launder the signal. Slice A instead proves no-behaviour-change via byte-identity (AC-6) and an unchanged-response test (AC-7).",
    "D3-shadow-location": "spatial_check registers as a SHADOW gate in the ENGINE facade, NOT inside quality_rubric.score_tour. This keeps tests/test_spatial_check.py::test_deliberately_not_wired_into_quality_rubric GREEN and unmodified, honouring the 2026-07-19 measured ruling instead of overruling it.",
    "D4-compose-script": "compose_script is NOT deleted in this ledger. Deleting it forces the phone onto per-stop Premium authoring, which multiplies phone cost ~8x and turns a single call into minutes — that cutover needs async job delivery and is its own slice. Here it is demoted to a named authoring mode reachable ONLY through the engine facade, pinned by test to exactly one call site. The firewall makes a second door impossible; the knob is removed when the phone cuts over.",
    "D5-tour-text-candidate": "scripts/tour_text_candidate.py is NOT dead and is NOT deleted. Verified 2026-07-26: scripts/tour_batch_candidate.py:18 and scripts/tour_batch_review.py:17 both import from it, and those back four paid Make targets. Only scripts/tour_text_candidate_review.py is dead. A permanent must-exist assertion guards this.",
    "D6-firewall-ratchet": "The firewall ships as a RATCHET, not an absolutist rule: a FROZEN_EXCEPTIONS literal naming today's remaining violators plus an assertion that the set never grows. An absolutist firewall cannot land without dragging in five unrelated modules; a ratchet lands immediately and makes regression impossible, which is the actual goal."
  },

  "acceptance_criteria": [
    { "id": "AC-1", "text": "Given an AST walk of src/ outside src/tour, when the firewall runs, then the only permitted src.tour.* imports are engine and contract plus a pinned FROZEN_EXCEPTIONS literal; the failure message names file, line and module." },
    { "id": "AC-2", "text": "Given a new deep tour import added to any src/ or scripts/ file, when the firewall runs, then it goes RED naming that exact file and line.", "negative": true },
    { "id": "AC-3", "text": "Given the FROZEN_EXCEPTIONS set, when a file is added to it, then a companion assertion fails because the set may only shrink.", "negative": true },
    { "id": "AC-4", "text": "Given tests/, when the firewall runs, then it is exempt by construction: the walker's root set is asserted to be exactly {src, scripts}." },
    { "id": "AC-5", "text": "Given an AST scan of src/ for narration-producing provider constructions, when the firewall runs, then exactly one module qualifies and equals a pinned literal; judge/verifier clients are excluded by an explicit pinned list, never by accident." },
    { "id": "AC-6", "text": "Given the facade and the raw premium chain driven with OfflinePremiumExecutor on identical input, when both run, then the resulting blueprint hashes are byte-identical — proving the facade is a facade and not a re-implementation." },
    { "id": "AC-7", "text": "Given a fixed preview request and the offline executor, when /trips/preview is called before and after the migration, then responses are byte-identical after removing only a pinned list of non-deterministic fields." },
    { "id": "AC-8", "text": "Given build_tour(phase=PLAN) with an executor whose execute() raises, then it returns route options and the executor is never invoked (zero calls on a counting stub).", "negative": true },
    { "id": "AC-9", "text": "Given build_tour(n_options=k) for k in {1,3}, then exactly k deterministically-ordered options are returned; k=0 raises ValueError; k above the feasible count returns all feasible without raising.", "negative": true },
    { "id": "AC-10", "text": "Given engine.ENGINE_GATES, then it equals a pinned literal in order AND the gates actually execute from it: reordering two entries changes the observed order of emitted findings." },
    { "id": "AC-11", "text": "Given engine.__all__, then it equals a pinned approved list; appending a name turns the firewall RED.", "negative": true },
    { "id": "AC-12", "text": "Given src/tour/engine.py and its exclusive call tree, then it contains no Neo4j write verb and no call into src/api/crud/ — persistence stays the caller's job.", "negative": true },
    { "id": "AC-13", "text": "Given /trips/{id}/compose, then it still returns TripComposeResponse with fresh stop ids, still 409s on already_composed, and still 422s with reason == compose_verification_failed — the exact shape mobile/lib/services/trip_service.dart:225 branches on." },
    { "id": "AC-14", "text": "Given the tree after deletion, then src/tour/author.py, tour_consistency.py, content_budget.py and the eight dead scripts are absent and unimportable, while scripts/tour_text_candidate.py, grade.py, audit.py, compose_metrics.py, quality_certification.py and quality_requests.py all still exist and import cleanly.", "negative": true },
    { "id": "AC-15", "text": "Given the deletions, when make _test-python is collected, then there are zero collection errors — the tests/conftest.py money-guard arms at :181 and :235 were removed in the same step as their modules." },
    { "id": "AC-16", "text": "Given compose_script after the migration, then exactly one call site reaches it and that call site is inside src/tour/engine.py; a second caller anywhere turns the firewall RED.", "negative": true },
    { "id": "AC-17", "text": "Given Severity, then a SHADOW member exists and a SHADOW finding lands in neither RubricReport.passed nor .warnings — the partition is unchanged for BLOCKER and WARN." },
    { "id": "AC-18", "text": "Given a tour containing a guaranteed-IMPLAUSIBLE look-cue, when the engine runs, then rubric.passed is True and rubric.warnings is empty while the shadow channel is non-empty, and the response JSON contains zero keys or values matching S1- or spatial.", "negative": true },
    { "id": "AC-19", "text": "Given the engine's spatial path with spatial_check._load_city_pois_cached monkeypatched to raise, then coordinates still resolve from the CorpusSnapshot — proving the .dockerignore'd data file is never consulted in production.", "negative": true },
    { "id": "AC-20", "text": "Given tests/test_spatial_check.py::test_deliberately_not_wired_into_quality_rubric, then it is still GREEN and its source is unmodified — the 2026-07-19 ruling was honoured, not overruled." },
    { "id": "AC-21", "text": "Given three structurally different induced authoring failures, then each yields a distinct typed CandidateRejection code — today two of them collapse into GENERATION_FAILED." },
    { "id": "AC-22", "text": "Given N previews of which F fell back, then a fallback counter and rate are observable; zero previews reports samples:0 rather than a divide-by-zero or a fabricated rate, and crossing the threshold emits exactly one ERROR per crossing, not per request.", "negative": true },
    { "id": "AC-23", "text": "Given a preview response with candidate_eligible:false and a populated candidate_rejection, when review.html renders, then the human-readable reason is visible and Basic stops are NOT rendered until an explicit control is clicked." },
    { "id": "AC-24", "text": "Given the source-text walk rendered on either surface, then the reader is told plainly that the AI wrote none of it and that the facts came from the source authors; and no surface implies it was AI-written. The honesty assertion is retargeted from the old literal strings to the new ones — the PROPERTY it protects is unchanged, only the wording it pins moves.", "negative": true },
    { "id": "AC-28", "text": "Given any message a human reads, in the workbench or the app, then it contains none of the banned internal vocabulary (build fingerprint, blueprint, traceability, candidate, certification, eligible, compose, provider, LLM, grounded, floored, degraded) — enforced by a test scanning the shipped frontend strings and the API's human-readable fields.", "negative": true },
    { "id": "AC-29", "text": "Given the renamed reason codes and wire fields, then every old name is absent from src/ and frontend/, and the response still deserialises for the existing Flutter client — the rename ships with whatever compatibility shim the client needs, not a silent break.", "negative": true },
    { "id": "AC-30", "text": "Given a walk that fails the quality standard, then no field or label anywhere states or implies that it passed; specifically the flag currently named candidate_eligible is renamed to say only what it means (every stop was fully written) and is never read as a quality verdict.", "negative": true },
    { "id": "AC-31", "text": "Given an 8-stop build where one stop's call raises and the rest succeed, then the successful stops are RETAINED and returned rather than discarded — today tuple(pool.map(...)) re-raises the first exception and throws away siblings that were already executed and already billed.", "negative": true },
    { "id": "AC-32", "text": "Given repeated transient failures, then the whole build makes at most 2 extra calls beyond one per stop (worst case 10 for an 8-stop walk); a deterministic content failure is never retried; and every attempt passes through the existing spend guard so the per-IP, hourly and daily ceilings still bind.", "negative": true },
    { "id": "AC-33", "text": "Given any completed build, then its real token usage and cost are persisted rather than discarded — the receipt hook already receives this data and currently drops it — and a failed build records what was spent before it failed.", "negative": false },
    { "id": "AC-25", "text": "Given the tourist lane, when the chosen route's authoring fails, then another route option is attempted before Basic is offered, and the ladder never retries the same route twice.", "negative": true },
    { "id": "AC-26", "text": "Given the test inventory document, then every path in its DELETE column is absent from git ls-files and every path in KEEP/ENHANCE is present, and every ADD entry names a node id that exists and passes." },
    { "id": "AC-27", "text": "Given the architecture diagram, then every name in the live engine.ENGINE_GATES appears in the diagram text — a diagram that disagrees with the code fails." }
  ],

  "steps": [
    {
      "id": "1", "name": "Delete the seven A/B harness scripts",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_engine_boundary.py::test_ab_harness_scripts_are_deleted\"",
      "criterion_ids": ["AC-14"],
      "files": ["scripts/author_ab.py", "scripts/polish_ab.py", "scripts/prose_ab.py", "scripts/tour_ab.py", "scripts/demo_full_tour.py", "scripts/factcheck_probe.py", "scripts/content_budget_report.py", "tests/test_tour_engine_boundary.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "low", "maxAttempts": 2, "attempts": 0,
      "depends_on": []
    },
    {
      "id": "2", "name": "Delete the Author Engine (module + runner + conftest money-guard arm + tests)",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_engine_boundary.py::test_author_engine_is_deleted\"",
      "criterion_ids": ["AC-14", "AC-15"],
      "files": ["src/tour/author.py", "scripts/author_tour.py", "scripts/phase_c_rerun.sh", "tests/test_author.py", "tests/conftest.py", "tests/test_tour_engine_boundary.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "normal", "maxAttempts": 3, "attempts": 0,
      "depends_on": ["1"]
    },
    {
      "id": "3", "name": "Delete the cross-stop consistency judge (module + conftest arm + tests)",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_engine_boundary.py::test_cross_stop_consistency_is_deleted\"",
      "criterion_ids": ["AC-14", "AC-15"],
      "files": ["src/tour/tour_consistency.py", "tests/test_tour_consistency.py", "tests/conftest.py", "tests/test_tour_engine_boundary.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "normal", "maxAttempts": 3, "attempts": 0,
      "depends_on": ["2"]
    },
    {
      "id": "4", "name": "Delete the content budget",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_engine_boundary.py::test_content_budget_is_deleted\"",
      "criterion_ids": ["AC-14"],
      "files": ["src/tour/content_budget.py", "tests/test_content_budget.py", "tests/test_tour_engine_boundary.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "low", "maxAttempts": 2, "attempts": 0,
      "depends_on": ["1"]
    },
    {
      "id": "5", "name": "Delete the superseded review runner; pin tour_text_candidate.py as load-bearing",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_engine_boundary.py::test_review_runner_deleted_and_candidate_runner_survives\"",
      "criterion_ids": ["AC-14"],
      "files": ["scripts/tour_text_candidate_review.py", "tests/test_tour_text_candidate_review_runner.py", "tests/test_tour_engine_boundary.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "low", "maxAttempts": 2, "attempts": 0,
      "depends_on": ["1"]
    },
    {
      "id": "6", "name": "Add Severity.SHADOW without moving the BLOCKER/WARN partition",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_quality_rubric.py::test_shadow_findings_are_neither_blockers_nor_warnings\"",
      "criterion_ids": ["AC-17"],
      "files": ["src/tour/quality_rubric.py", "tests/test_tour_quality_rubric.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "low", "maxAttempts": 2, "attempts": 0,
      "depends_on": ["3", "4", "5"]
    },
    {
      "id": "7", "name": "Create src/tour/engine.py with frozen __all__ and ENGINE_GATES",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_engine_boundary.py::test_engine_all_equals_the_approved_list\"",
      "criterion_ids": ["AC-11"],
      "files": ["src/tour/engine.py", "tests/test_tour_engine_boundary.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "normal", "maxAttempts": 2, "attempts": 0,
      "depends_on": ["6"]
    },
    {
      "id": "8", "name": "build_tour(PLAN_AND_AUTHOR) delegates to the premium chain, byte-identical blueprint",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_engine_facade.py::test_plan_and_author_matches_premium_chain_byte_for_byte\"",
      "criterion_ids": ["AC-6"],
      "files": ["src/tour/engine.py", "tests/test_tour_engine_facade.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "high", "maxAttempts": 2, "attempts": 0,
      "depends_on": ["7"]
    },
    {
      "id": "9", "name": "build_tour(PLAN) returns n options and makes zero provider calls",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_engine_facade.py::test_plan_phase_returns_n_options_and_spends_nothing\"",
      "criterion_ids": ["AC-8", "AC-9"],
      "files": ["src/tour/engine.py", "tests/test_tour_engine_facade.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "normal", "maxAttempts": 2, "attempts": 0,
      "depends_on": ["8"]
    },
    {
      "id": "10", "name": "Engine performs no persistence (no Neo4j write verb, no crud import)",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_engine_boundary.py::test_engine_performs_no_persistence\"",
      "criterion_ids": ["AC-12"],
      "files": ["src/tour/engine.py", "tests/test_tour_engine_boundary.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "low", "maxAttempts": 2, "attempts": 0,
      "depends_on": ["9"]
    },
    {
      "id": "11", "name": "Migrate preview_trip to engine.build_tour",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_premium_workbench_wiring.py::test_preview_uses_shared_premium_plan_and_finalizer\"",
      "criterion_ids": ["AC-7"],
      "files": ["src/api/routes/trips.py", "tests/test_premium_workbench_wiring.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "high", "maxAttempts": 3, "attempts": 0,
      "depends_on": ["10"]
    },
    {
      "id": "12", "name": "Migrate generate_trip to Phase.PLAN",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_trip_preview_contract.py::test_generate_route_holds_no_direct_selection_imports\"",
      "criterion_ids": ["AC-1", "AC-9"],
      "files": ["src/api/routes/trips.py", "tests/test_trip_preview_contract.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "high", "maxAttempts": 3, "attempts": 0,
      "depends_on": ["11"]
    },
    {
      "id": "13", "name": "Migrate compose_trip through the facade, preserving the 422 contract the phone branches on",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_recompose.py::test_compose_goes_through_the_engine_and_keeps_the_422_contract\"",
      "criterion_ids": ["AC-13", "AC-16"],
      "files": ["src/api/routes/trips.py", "tests/test_tour_recompose.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "high", "maxAttempts": 3, "attempts": 0,
      "depends_on": ["12"]
    },
    {
      "id": "14", "name": "Reduce trips.py to engine + contract imports only",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_engine_boundary.py::test_trips_route_imports_only_engine_and_contract\"",
      "criterion_ids": ["AC-1"],
      "files": ["src/api/routes/trips.py", "tests/test_tour_engine_boundary.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "normal", "maxAttempts": 3, "attempts": 0,
      "depends_on": ["13"]
    },
    {
      "id": "15", "name": "Pin compose_script to exactly one call site, inside the engine",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_engine_boundary.py::test_legacy_composer_has_exactly_one_call_site_in_the_engine\"",
      "criterion_ids": ["AC-16"],
      "files": ["src/tour/engine.py", "tests/test_tour_engine_boundary.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "normal", "maxAttempts": 2, "attempts": 0,
      "depends_on": ["14"]
    },
    {
      "id": "16", "name": "Firewall (a): src/ may import only engine + contract, with a shrink-only exception set",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_engine_boundary.py::test_src_imports_only_engine_and_contract\"",
      "criterion_ids": ["AC-1", "AC-2", "AC-3"],
      "files": ["tests/test_tour_engine_boundary.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "normal", "maxAttempts": 3, "attempts": 0,
      "depends_on": ["14"]
    },
    {
      "id": "17", "name": "Firewall (b): same rule for scripts/",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_engine_boundary.py::test_scripts_import_only_engine_and_contract\"",
      "criterion_ids": ["AC-1", "AC-2", "AC-3"],
      "files": ["tests/test_tour_engine_boundary.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "normal", "maxAttempts": 2, "attempts": 0,
      "depends_on": ["16"]
    },
    {
      "id": "18", "name": "Firewall (c): tests/ exempt by construction, not by luck",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_engine_boundary.py::test_tests_directory_is_exempt_by_construction\"",
      "criterion_ids": ["AC-4"],
      "files": ["tests/test_tour_engine_boundary.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "low", "maxAttempts": 2, "attempts": 0,
      "depends_on": ["17"]
    },
    {
      "id": "19", "name": "Firewall (d): exactly one module constructs a narration provider call",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_engine_boundary.py::test_exactly_one_narration_provider_constructor\"",
      "criterion_ids": ["AC-5"],
      "files": ["tests/test_tour_engine_boundary.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "normal", "maxAttempts": 3, "attempts": 0,
      "depends_on": ["15"]
    },
    {
      "id": "20", "name": "Firewall (e): gate tuple equals the literal AND the gates execute in that order",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_engine_boundary.py::test_gates_execute_in_declared_order\"",
      "criterion_ids": ["AC-10"],
      "files": ["tests/test_tour_engine_boundary.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "normal", "maxAttempts": 2, "attempts": 0,
      "depends_on": ["19"]
    },
    {
      "id": "21", "name": "CorpusSnapshot to city_pois adapter that never reads the dockerignored data file",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_spatial_check.py::test_city_pois_come_from_the_snapshot_not_the_repo_file\"",
      "criterion_ids": ["AC-19"],
      "files": ["src/tour/engine.py", "tests/test_spatial_check.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "normal", "maxAttempts": 2, "attempts": 0,
      "depends_on": ["20"]
    },
    {
      "id": "22", "name": "Register spatial_check as a SHADOW gate that blocks nothing and surfaces nothing",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_quality_rubric.py::test_spatial_gate_is_shadow_and_never_flips_passed\"",
      "criterion_ids": ["AC-18", "AC-10"],
      "files": ["src/tour/engine.py", "tests/test_tour_quality_rubric.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "normal", "maxAttempts": 3, "attempts": 0,
      "depends_on": ["21"]
    },
    {
      "id": "23", "name": "Repoint the quality_rubric S1 rationale at the shadow slot, keeping the evidence and the C4 guard green",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_spatial_check.py::test_deliberately_not_wired_into_quality_rubric\"",
      "criterion_ids": ["AC-20"],
      "files": ["src/tour/quality_rubric.py", "tests/test_tour_quality_rubric.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "low", "maxAttempts": 2, "attempts": 0,
      "depends_on": ["22"]
    },
    {
      "id": "24", "name": "Every fallback carries a distinct typed reason",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_trips_spend_and_authz.py::test_each_fallback_carries_a_distinct_typed_reason\"",
      "criterion_ids": ["AC-21"],
      "files": ["src/tour/engine.py", "tests/test_trips_spend_and_authz.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "normal", "maxAttempts": 2, "attempts": 0,
      "depends_on": ["20"]
    },
    {
      "id": "25", "name": "Fallback-rate counter with an explicit zero-sample case and one alarm per crossing",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_trips_spend_and_authz.py::test_fallback_counter_rate_and_alarm\"",
      "criterion_ids": ["AC-22"],
      "files": ["src/tour/engine.py", "tests/test_trips_spend_and_authz.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "normal", "maxAttempts": 2, "attempts": 0,
      "depends_on": ["24"]
    },
    {
      "id": "26", "name": "Editor lane: the reason is visible and Basic becomes opt-in",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_workbench_review_regressions.py::test_basic_lane_names_the_rejection_and_requires_opt_in\"",
      "criterion_ids": ["AC-23", "AC-24"],
      "files": ["frontend/review.html", "tests/test_workbench_review_regressions.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "normal", "maxAttempts": 3, "attempts": 0,
      "depends_on": ["24"]
    },
    {
      "id": "27", "name": "Tourist lane: another route option before Basic, with the honest label preserved verbatim",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_preview_page.py::test_tourist_lane_offers_another_route_before_basic\"",
      "criterion_ids": ["AC-25", "AC-24"],
      "files": ["frontend/tour-preview.html", "tests/test_preview_page.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "normal", "maxAttempts": 2, "attempts": 0,
      "depends_on": ["24"]
    },
    {
      "id": "28", "name": "Test inventory document, cross-checked against the real git deletions",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_engine_boundary.py::test_test_inventory_matches_the_tree\"",
      "criterion_ids": ["AC-26"],
      "files": ["specs/2026-07-26-tour-engine-convergence/06-test-inventory.md", "tests/test_tour_engine_boundary.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "normal", "maxAttempts": 2, "attempts": 0,
      "depends_on": ["20"]
    },
    {
      "id": "30", "name": "Rename the reason codes and the two actively misleading names",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_engine_boundary.py::test_no_banned_internal_vocabulary_in_reason_names\"",
      "criterion_ids": ["AC-28", "AC-29"],
      "files": ["src/tour/candidate_eligibility.py", "src/api/models/trips.py", "tests/test_tour_engine_boundary.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "high", "maxAttempts": 3, "attempts": 0,
      "depends_on": ["24"]
    },
    {
      "id": "31", "name": "Stop the eligibility flag reading as a quality verdict",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_trip_preview_contract.py::test_ai_written_flag_is_not_a_quality_verdict\"",
      "criterion_ids": ["AC-30"],
      "files": ["src/api/routes/trips.py", "src/api/models/trips.py", "tests/test_trip_preview_contract.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "normal", "maxAttempts": 2, "attempts": 0,
      "depends_on": ["30"]
    },
    {
      "id": "32", "name": "No banned vocabulary reaches any human-readable string",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_engine_boundary.py::test_no_banned_vocabulary_in_user_facing_strings\"",
      "criterion_ids": ["AC-28"],
      "files": ["tests/test_tour_engine_boundary.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "normal", "maxAttempts": 2, "attempts": 0,
      "depends_on": ["26", "27", "31"]
    },
    {
      "id": "33", "name": "Keep the stops we already paid for when one fails",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_trips_spend_and_authz.py::test_a_failed_stop_does_not_discard_the_paid_ones\"",
      "criterion_ids": ["AC-31"],
      "files": ["src/tour/premium_tour.py", "tests/test_trips_spend_and_authz.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "high", "maxAttempts": 3, "attempts": 0,
      "depends_on": ["25"]
    },
    {
      "id": "34", "name": "Cap retries at two extra calls per build, transient causes only",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_trips_spend_and_authz.py::test_retry_budget_is_two_and_content_failures_are_never_retried\"",
      "criterion_ids": ["AC-32"],
      "files": ["src/tour/premium_tour.py", "tests/test_trips_spend_and_authz.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "high", "maxAttempts": 3, "attempts": 0,
      "depends_on": ["33"]
    },
    {
      "id": "35", "name": "Record what each build actually cost",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_trips_spend_and_authz.py::test_build_cost_is_recorded_including_on_failure\"",
      "criterion_ids": ["AC-33"],
      "files": ["src/tour/premium_tour.py", "tests/test_trips_spend_and_authz.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "normal", "maxAttempts": 2, "attempts": 0,
      "depends_on": ["34"]
    },
    {
      "id": "29", "name": "Architecture diagram regenerated from the live gate list",
      "status": "pending",
      "test_command": "make test-file FILE=\"tests/test_tour_engine_boundary.py::test_architecture_diagram_names_every_engine_gate\"",
      "criterion_ids": ["AC-27"],
      "files": ["Docs/tour-engine/architecture.svg", "tests/test_tour_engine_boundary.py"],
      "cost_class": "$0", "cost": "$0", "complexity": "low", "maxAttempts": 2, "attempts": 0,
      "depends_on": ["22", "27"]
    }
  ]
}
