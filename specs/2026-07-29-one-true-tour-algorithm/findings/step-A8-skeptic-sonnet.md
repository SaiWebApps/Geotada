# Step A8 skeptic review — sonnet — FIX CORRECTNESS angle

Verified against working tree on top of commit `c8ec3969` (HEAD, clean-except-A8-in-progress; `tests/conftest.py` and `specs/.../state.json`/`run-context.md` show MM = staged+unstaged, consistent with a concurrent in-flight step). `state.json`'s A8 entry currently reads `status: in_progress`, `attempts: 0`, stale `proof: "dependency not completed"` — the ledger itself has not yet recorded the proof text this CLAIM paraphrases, so I verified the CLAIM against the actual diff on disk rather than against a committed proof.

## What I independently re-derived (not trusted from the claim)

- `make lint` — ran myself: exit 0, "All checks passed!".
- Confirmed all 12 `COMPOSER_DELETED_FILES` are gone from disk (none printed by an existence sweep) and zero remaining `.py` file anywhere in the repo (not just the 4 scanned roots) imports `src.tour.compose` or `src.tour.factcheck`.
- Confirmed `src/tour/compose_gate.py`'s public top-level surface is now exactly `{ComposeVerificationError, build_full_verifier}` via my own `ast.parse` — matches `REDUCED_COMPOSE_GATE_API` exactly.
- Confirmed no surviving file imports the deleted compose_gate ladder names (`compose_and_verify`, `serve_or_block`, `drop_failing_sentences`, `repair_composed*`, `MAX_COMPOSE_ATTEMPTS`, `ComposeFn`/`VerifyFn`) — only stale comment mentions remain (see finding 4).
- Confirmed `src/api/dependencies.py` no longer defines `get_compose_client`/`get_omission_checker` (A8's half of AC-9(e)); `get_correction_client`/`get_claim_repetition_judge` correctly remain (A9's half, per the test file's own docstring).
- Confirmed `tests/conftest.py`'s `_money_guard_no_live_compose` fixture: the compose-client (`AnthropicComposeClient`/`OpenAIComposeClient`) and factcheck (`HaikuClaimDecomposer`/`HaikuCoverageJudge`/`HaikuFaithfulnessJudge`) arms are removed; `PREMIUM_FAITHFULNESS_ARM_SOURCE` is byte-for-byte present in the current `conftest.py` (verified with a literal substring check against the AST-extracted constant, not eyeballing).
- Confirmed `Makefile`'s `LIVE_TEST_FILES` no longer lists `tests/test_tour_compose_live.py`, and no other Makefile target references any of the 10 deleted test files.
- Confirmed `compose_script`/`compose_script_per_chapter` were real entry points in the pre-A8 `compose.py` (`git show HEAD:...`), so `WHOLE_TOUR_ENTRY_POINTS` is not a strawman name — it is the actual boundary the old code exposed.
- The pinned test (`test_whole_tour_composer_is_gone`) is structural (AST + `git ls-files` + real Makefile-syntax parsing), never lexical/regex — consistent with this repo's explicit ban on lexical shortcuts for meaning-level checks, and a genuine improvement over a text-grep gate.
- Reconciled the mutation evidence's own diffstat claim (`Makefile 3, tour_build.py 38, dependencies.py 68, compose_gate.py 285, narration_quality.py 25, conftest.py 75`) against `git diff --stat` myself on those exact 6 files — matches exactly.

**Conclusion on the pinned mutation:** the two RED runs in the evidence are legitimate and encode real failure modes (an ImportError from a stale `compose` import, and — once collection is unblocked — the test's own first assertion, `on_disk` check, firing with the exact message format at test_tour_one_engine.py:423). However the mutation only ever exercised check #1 of the six in the test body; checks #2-6 (import-boundary, compose_gate API-surface equality, conftest arm equality, dependencies.py removal, Makefile parsing) were never independently driven RED by the QA mutation — they were asserted correct only by the developer's own claim. I closed that gap myself with the independent static re-derivations above, all of which hold on the current tree.

## Findings

### 1. D9(5) consented loss not executed: `craft_score` is now dead code, left in place (MEDIUM, CONFIRMED)

D9(5) explicitly consents, by the owner's "go", to losing "the compose-metrics scoreboard + craft_score + `/report-tour-issue` skill". A7's own judge ruling (in `state.json`, A7's `proof` field) recorded this precisely: A7 could not delete `craft_score` because `compose.py:84` was still a real caller at that time, and explicitly wrote *"CF-5 records A8 as its owner"* for finishing this deletion once `compose.py` was gone. (Note: CF-5 itself was never promoted into `state.json`'s top-level `carry_forward` array — it exists only inside A7's prose — which is presumably why it fell through.)

`compose.py` is deleted in this very A8 diff. I confirmed by repo-wide grep that `craft_score` now has **zero callers** anywhere (`src/`, `scripts/`, `tests/`, `tools/`) except its own definition and its own ~5-6 dedicated tests in `tests/test_narration_quality.py`. Instead of deleting it, A8's diff only rewrites `craft_score`'s docstring to say: *"compose.py itself is scheduled for deletion at A8, after which this function has zero production callers."* That "after" state is the CURRENT state within this same diff — the docstring narrates a future event that has already happened, and the described consequence (zero callers) is left unaddressed. `narration_quality.py` as a *file* cannot be deleted (its sibling `score_narration` is load-bearing for `quality_rubric.py`, `src/api/routes/trips.py`, `scripts/score_gold_text.py` — all D8-kept/live), so this is specifically about the `craft_score` function (plus whatever private G2/G3 helpers exist only for it) and its dedicated tests, not the whole module.

This does not break `make lint` or the pinned test (neither checks for `craft_score`), so it is not a reproducible test failure — I did not fabricate one. But it is a real, verifiable non-compliance with the ledger's own explicit consented-loss list and its own carry-forward assignment, and it leaves the tree in a self-contradicting state (a docstring describing a "will happen" that already happened, without doing it).

- repro (read-only, I ran this myself): `grep -rln "craft_score" . --include="*.py" | grep -v '\.git/'` → only `src/tour/narration_quality.py` and `tests/test_narration_quality.py` (plus a comment mention in `tests/test_tour_one_engine.py`). Zero production callers confirmed.

### 2. `scripts/tour_build.py`'s changed failure-path behavior is functionally untested (LOW, UNPROVEN)

Before A8: on `ComposeVerificationError`, `tour_build.py` printed an error and `return 1` **before** writing any JSON/Markdown output. After A8: the script now always calls `verify(script)`, always writes `json_path`/`md_path`, and only decides the exit code (`0 if passed else 1`) at the very end — so a failing VERIFY report now produces on-disk artifacts it never used to. This is a reasonable design choice (D7 doesn't prohibit it) but it is a genuine behavior change, and there is **no test file for `scripts/tour_build.py`** (before or after this change — confirmed: `tests/` has no `test_tour_build*.py`), and `make tour-build` is not part of `make test` or `make audit`'s shard list (confirmed by reading the Makefile's `tour-build:` target and `run-context.md`'s CG-1 shard list). AC-10's "make tour-build runs the $0 stitch+render harness" clause is asserted in the pinned test only *structurally*, through `compose_gate`'s reduced API surface — the test file's own docstring says so explicitly ("asserted through the reduced compose_gate surface ... NOT run"). So nothing in this ledger will ever actually execute `scripts/tour_build.py` end-to-end to confirm it still runs.
- I did NOT run `make tour-build` myself — it starts `_ensure-dev-data` + `valhalla-up` against the shared dev Neo4j (7687) and the shared Valhalla container, which falls in the same "do not run concurrently with sibling skeptics" bucket as the other proposed-only commands.
- repro_command (propose only, not run by me): `make tour-build` (or a narrower invocation via `ARGS=` pointing at a small city) — the serial verifier should confirm it still exits 0/1 correctly and produces output.

### 3. Claim's "satisfies AC-1, AC-9, AC-10" risks reading as full closure (LOW, advisory / documentation-hygiene, not a functional break)

Per the ledger's own CF-4 doctrine (and the pinned test file's own docstring, which is candid about this), A8 only closes SOME clauses of each named AC: AC-9(e)'s `get_correction_client`/`get_claim_repetition_judge` clauses are explicitly A9's; AC-10's docs/spec-folder/`render.yaml`/`pyproject.toml` clauses are explicitly A10's (confirmed: none of those paths are in A8's `files[]` in `state.json`, all are in A10's). A6 and A7's own proof texts each explicitly stated "THIS IS NOT AC-1/AC-9 MET" to avoid exactly this ambiguity. Whatever proof text eventually lands for A8 needs the same explicit disclaimer, or a reader could mistake "criterion_ids: [AC-1, AC-9, AC-10]" for full closure. This is not something I can "break" with a repro — it is a wording/audit-trail risk, flagged because the pattern established by A6/A7 makes its absence conspicuous.

### 4. Stale comment residue outside A8's scope (LOW, informational, not A8's to fix)

`src/tour/quality_rubric.py:859`, `tests/test_tour_quality_rubric.py:1118` cite `repair_composed_surgical (compose_gate.py:125)` — a function A8 deletes; `src/tour/candidate_eligibility.py:83` and `src/tour/contract.py:727` mention `compose_script_per_chapter` in comments; `src/tour/claim_repetition.py:343` and `tests/test_spatial_check.py`/`quality_rubric.py` mention `HaikuFaithfulnessJudge`/`SemanticFactChecker` by name. All are comment-only (not AST import/attribute edges, so the pinned test correctly does not flag them) and all sit in D8-protected or A9/A10-scope files, so this is not A8's fault to fix — but it is exactly the "doc contradicts the code" pattern this repo's own CLAUDE.md flags, worth a carry-forward note for A9/A10's sweep. `pyproject.toml`'s pytest `addopts` also still has `--ignore=tests/test_tour_compose_live.py` (harmless no-op, but stale) — correctly deferred, since `pyproject.toml` is in A10's `files[]`, not A8's.

## Overall verdict on the CLAIM

The structural deletion work itself (D6's "compose.py + factcheck.py + compose_gate ladder + Makefile LIVE_TEST_FILES at A8 in one step") is CONFIRMED correct against everything I could statically re-derive: file deletions, import-boundary, compose_gate's reduced API, conftest arm removal + byte-identity of the surviving premium arm, dependencies.py's A8-half of AC-9(e), and Makefile pruning all check out independently, and the pinned test is a genuine, well-designed, non-lexical red-first proof (not a strawman) for the six things it actually asserts. `make lint` is clean (verified myself, exit 0).

However, the CLAIM as stated overreaches on two points a careful reader would need corrected before this is signed off as "done": (1) a real, verifiable gap against the ledger's OWN explicit plan (D9(5) + CF-5) — `craft_score` should have been deleted as dead code in this step and was not; and (2) an unproven functional claim about `scripts/tour_build.py` that no test in this ledger, at any step, will ever exercise. Neither is a broken test today, so I am not blocking on a fabricated repro — but finding 1 is CONFIRMED via direct, reproducible grep evidence, not advisory-only.
