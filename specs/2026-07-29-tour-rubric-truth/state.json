{
  "topic": "tour-rubric-truth",
  "created": "2026-07-29",
  "tier": 2,
  "approved_by_human": true,
  "approved_at": "2026-07-30T02:24:53Z",
  "approval_note": "Owner said 'go' in chat after the full presentation. The go bundles: C8 stays 850 + standing-position unit ruling (closes the open C8-unit question), C9 unchanged, C11 demoted to report-only stat, overlap floors restated at 85% of resolved overlap, and the runs-first-from-worktree order.",
  "run": {
    "last_run": "2026-07-30T02:37:25Z (engine) + manual repair session",
    "engine_result": "The engine BLOCKED at B1 'gate still red at max attempts' and skipped B2-B5. Correctly: its QA pass refused to bless the change because it had independently confirmed a real fail-open (grade.py scoring a zero-match tour 1.00). paid_gate_runs 0, 15 agents, 32/213 units. The re-key work itself was sound and mutation-proven REAL by that same QA pass.",
    "completed_manually_after": "The 3 QA-named defects plus 5 more from a 2-model hostile panel were fixed by the main agent, each mutation-proven, then independently attacked. Steps are marked completed on RUN evidence, not on assertion - see each proof field.",
    "verification": "make lint clean; make _test-python 2591 passed; make _test-golden 9 passed (RED at baseline); make _test-grade 15 passed; make golden-probe prints both markers, exit 0 (was Error 1); make score-human-tours 0 BLOCKERS at position granularity on both human references; make score-gold-text 'the gold passes every applicable check'; 60 rubric + 47 spatial tests green.",
    "paid_gate_runs": 0,
    "scope_added_beyond_the_approved_files": [
      "src/tour/grade.py - the re-key deleted the key it reads; two fail-opens fixed",
      "scripts/tour_golden_diff.py - left crashing on the deleted key (make golden-diff)",
      "Makefile - golden-probe recipe; it only ever worked while the gate was red",
      "Docs/tour-builder/golden-fixture-repair/README.md - two now-false claims corrected"
    ],
    "open_for_the_owner": "PdV's floor is 4 hits, EQUAL to the measurement, so a single lost hit reds the gate. A judge consult rejected the D6-derived 3 as near-vacuous at n=4 (85% of 4 rounds to 3, shrugging at a 25% regression) and noted the gate is deterministic by construction, so there is no non-determinism to buy slack for. The remaining honest gap is the DISTANCE to the 90% target: the engine selects only FOUR beats at Place des Vosges and spends the rest of the budget on two other POIs, while all 17 misses are arcade addresses at that one square. Closing it means sub-location stops (the same question D4 rules on for C8), not moving the floor. Per the anti-spin order the algorithm was NOT touched.",
    "judge_requested_fixes_applied": [
      "The comments in both golden modules cited test_golden_probe_marker_is_emitted_by_both_goldens, which DID NOT EXIST \u2014 the phantom-guard pattern this repo has been burned by. The test is now written and asserts both the marker in each golden module and the grep in the Makefile recipe.",
      "AC-6 was unmet: specs/2026-07-19-tour-quality-standard/01-standard.md's C11 row still said 'report + WARN on outliers | WARN' while the code emits nothing. Corrected. selection.py needed no edit \u2014 it already carries its own 2026-07-27 self-correction and matches the shipped constants.",
      "Ledger attempts were all '1', which was false for B1 (blocked at 2) and fabricated for B2-B5 (never attempted). Now per-step accurate with engine_outcome and completed_by recorded.",
      "test_golden_diff_cli_reads_the_durable_key was a source-text check that would pass on a module that raises on import; it now imports scripts.tour_golden_diff for real.",
      "scripts/tour_golden_diff.py was outside the make lint file list (its edit was unlinted, and ruff reported I001); added to the list and fixed."
    ],
    "no_op_clarification": "Nothing is DELETED by this commit. fixtures/tour_golden/repair-input/ was already removed from the tree at aafc4fb7; the engine's developer re-added it from git 4ea26683, which created a byte-identical duplicate of the tracked Docs/tour-builder/golden-fixture-repair/. Removing that re-addition is a no-op on the diff against HEAD, not a data removal."
  },
  "baseline": {
    "lint": "make lint -> All checks passed! (2026-07-29)",
    "commit": "RUNS FIRST, FROM A WORKTREE: $HOME/git/ondoway-rubric, branch cut from current main (with both approved spec ledgers committed) BEFORE the sibling consolidation ledger runs. The engine is invoked with repo=$HOME/git/ondoway-rubric. On completion: owner reviews + commits on the branch, merge to main --ff-only, delete the worktree + branch, and only then does the sibling start on the now-golden-green main. Never run concurrently with the sibling or any test suite (shared 7687/7688/Valhalla). Steps B1-B3 are deliberately packed so the goldens are green by the FIRST engine phase gate (which hard-runs _test-golden after 3 steps).",
    "note": "make _test-golden and the ile grade node are RED at baseline BY DESIGN \u2014 this ledger's whole job is to turn them green honestly. Root cause (measured): fixtures pin per-database ephemeral UUIDs (b.id, minted by randomUUID() at upload) \u2014 0/65 exist in data/paris/beats.json, whose durable ids are slugs (b.beat_id / stable_beat_id). Never re-record fresh UUIDs (that laundering already happened once, commit 3d62713d) and never lower a floor to whatever the run produces."
  },
  "decisions": {
    "D1-durable-ids": "Fixtures re-key to expected_stable_beat_ids (slugs). The golden tests and grade test map generated source_id -> stable_beat_id via the BeatSequence they already hold (BeatRef carries both fields). Zero production-code change.",
    "D2-resolution-method": "Tag->beat resolution re-runs the fixtures' own script_body method from the DOCUMENTS, exact-slug-suffix FIRST (24/93 resolve uniquely today), similarity only as fallback. The repair-input recovered from git 4ea26683 holds 64 DISTINCT machine-similarity resolutions (2 known-conflicting) \u2014 they are hints, NOT human-vetted truth: similarity has measured wrong-beat failures at 0.909 and 1.000. PdV additionally needs its 25 fixture keys re-keyed to the document's real tags (only 8 match verbatim today). Unresolved tags go into a human-review queue file; nothing is silently auto-resolved.",
    "D3-invariants-over-resolved-subset": "The internal-consistency test asserts over the RESOLVED subset: resolved ids are unique, all exist in data/paris/beats.json, count matches the restated expected_beat_count, and expected_tag_resolution covers the reachable set. It does NOT demand 100% tag resolution \u2014 the queue is a first-class output.",
    "D4-c8-ruling": "C8 KEEPS cap 850 per engine stop \u2014 the standing in-repo measurement (quality_rubric.py:59-62) rejected 750 because the certified-good corpus has stops at 757/761/808, and moving the cap re-blocks them for zero gain on new output (the engine's 270s dwell ceiling renders ~750 max anyway). ADDED: the unreachability pin test (a clean C8 on fresh output is the dwell ceiling at work, never evidence of a quality win), and the unit ruling recorded in the two decision-forcer tests: the human-reference calibration unit is the STANDING POSITION (human max 367 \u2014 all pass); declared-stop aggregates (Conciergerie 901, Notre-Dame 1595) are eye-prose spans over many positions and are not a stop the engine builds. This CLOSES the open C8-unit question.",
    "D5-c9-c11": "C9 stays EXACTLY as-is (WARN, cap 20): quality_rubric.py:80-85 documents it as sound \u2014 it ranks the owner's gold (19.11) above machine output (median 23.2) and matches the online 12-18-word ear-prose guidance; it fires on the empirical references because they are guidebook EYE-prose, recorded as by-design in the calibration expectation test. C11 is demoted to a report-only stat: it fires on 100% of human-vetted references but only 8-11% of machine tours \u2014 an inverted signal; the new decision-forcer test records the measurement. Net rubric behavior change across this whole ledger: C11 stops emitting findings. Nothing else moves. This is the anti-spin worst case honored: the algorithm and the rubric's blockers ship as-is.",
    "D6-floors": "Overlap floors are restated deliberately against the resolved set (recommendation: floor = 85% of the resolved-tag overlap measured at repair time; today's floors of 42.6%/27.8% permit 57-72% silent drift and cannot fail). GRADE_BASELINE 0.65 is reconfirmed against the repaired fixtures. Owner may override the numbers at approval; approving the plan approves the recommendation.",
    "D7-disjointness": "This ledger touches NO file the sibling consolidation ledger touches. The one shared file (specs/2026-07-19-tour-quality-standard/01-standard.md) is resolved by sequencing: THIS ledger edits \u00a74/\u00a75 rows first (it runs first and merges); the sibling deletes \u00a76b afterwards on a main that already contains those edits. No concurrent edits, no merge conflict. quality_rubric.py, selection.py (comment only), spatial_check.py are this ledger's alone."
  },
  "acceptance_criteria": [
    {
      "id": "AC-1",
      "text": "Given both golden fixtures, then every expected_stable_beat_id is a slug that exists in data/paris/beats.json, zero UUID-shaped ids remain, and the internal-consistency test over the resolved subset is green (unique, counted, tag-resolution-covered)."
    },
    {
      "id": "AC-2",
      "text": "Given the resolution rerun, then every exact-slug-suffix match is used before any similarity fallback, the 2 known-conflicting repair-input mappings are re-adjudicated rather than inherited, and a human-review queue file exists listing exactly the still-unresolved tags \u2014 no silent auto-resolution.",
      "negative": true
    },
    {
      "id": "AC-3",
      "text": "Given the live dev graph (7687) and Valhalla, then tests/test_tour_golden_ile.py::test_ile_golden_overlap, tests/test_tour_golden_pdv.py::test_pdv_golden_overlap and tests/test_tour_grade.py::test_broken_golden_drops_below_baseline are green, with floors restated per D6 \u2014 never lowered to whatever the run produced.",
      "negative": true
    },
    {
      "id": "AC-4",
      "text": "Given the C8 ruling, then the cap is unchanged at 850, the unreachability pin test is green (MAX_DWELL_AUDIO_SECONDS=270 makes C8 inert on fresh engine output), and the two former decision-forcer tests record the standing-position unit ruling instead of forcing."
    },
    {
      "id": "AC-5",
      "text": "Given the C9/C11 ruling, then C9's behavior and cap are byte-unchanged with the eye-prose firing recorded as by-design, C11 emits a report stat and zero Findings, and make score-human-tours shows zero BLOCKER findings on the human references at position granularity while the gold text still passes everything applicable.",
      "negative": true
    },
    {
      "id": "AC-6",
      "text": "Given the stale claims found by measurement, then spatial_check.py's docstring no longer claims S1 is wired into score_tour, selection.py's drift comment is corrected, and standard \u00a74/\u00a75 rows match the shipped constants \u2014 no doc contradicts the code.",
      "negative": true
    }
  ],
  "steps": [
    {
      "id": "B1",
      "name": "Re-key the golden fixtures to durable slug ids; restore repair-input from git 4ea26683; internal-consistency test over the resolved subset",
      "status": "completed",
      "test_command": "make test-file FILE=\"tests/test_tour_golden_consistency.py::test_fixture_ids_are_durable_slugs_and_internally_consistent\"",
      "criterion_ids": [
        "AC-1"
      ],
      "files": [
        "fixtures/tour_golden/ile_oneway_90min.json",
        "fixtures/tour_golden/pdv_round_trip_60min.json",
        "fixtures/tour_golden/repair-input",
        "tests/test_tour_golden_consistency.py",
        "tests/test_tour_golden_ile.py",
        "tests/test_tour_golden_pdv.py",
        "tests/test_tour_grade.py"
      ],
      "cost_class": "$0",
      "cost": "$0",
      "complexity": "high",
      "maxAttempts": 3,
      "attempts": 2,
      "depends_on": [],
      "proof": "COMPLETED. Both fixtures re-keyed to expected_stable_beat_ids (durable corpus slugs); 0 UUID-shaped ids remain; 3 consistency tests green incl. a resolution ratchet (ile>=51, pdv>=24) that closes the 'queue every tag and pass' hole, and a pinned-set-size test that forces the overlap floor to be re-derived on growth (hostile review proved growth silently weakens an absolute floor). repair-input was NOT restored: an identical tracked copy already existed at Docs/tour-builder/golden-fixture-repair/, so restoring it created a byte-identical duplicate (verified by sha256) - the duplicate was deleted and the tracked copy's two now-false claims corrected instead. Mutations: ratchet +1 -> RED; pinned-size -1 -> RED.",
      "commit": "pending",
      "completed_by": "main-agent manual repair after the engine blocked at B1; verified by a 2-model hostile skeptic panel and a judge consult. The engine itself completed NO step.",
      "engine_outcome": "BLOCKED after 2 of 3 attempts: 'gate still red at max attempts'. Its QA pass was RIGHT to refuse \u2014 it had independently confirmed the grade.py fail-open."
    },
    {
      "id": "B2",
      "name": "Re-run tag resolution from the documents: exact-slug-suffix first, similarity fallback, PdV keys re-keyed to the document's real tags, human queue for the rest",
      "status": "completed",
      "test_command": "make test-file FILE=\"tests/test_tour_golden_consistency.py::test_every_resolved_tag_exists_in_the_corpus\"",
      "criterion_ids": [
        "AC-2"
      ],
      "files": [
        "fixtures/tour_golden/ile_oneway_90min.json",
        "fixtures/tour_golden/pdv_round_trip_60min.json",
        "fixtures/tour_golden/repair-input",
        "tests/test_tour_golden_consistency.py"
      ],
      "cost_class": "$0",
      "cost": "$0",
      "complexity": "high",
      "maxAttempts": 3,
      "attempts": 0,
      "depends_on": [
        "B1"
      ],
      "proof": "COMPLETED. Resolution re-run from the documents, exact-slug-suffix first: PdV 8/25 real tag keys -> 24/25 resolved, Ile 51/70. 20 unresolved tags queued in each fixture's unresolved_tags; an independent skeptic verified all 20 have ZERO exact-suffix matches in data/paris/beats.json, so nothing resolvable was hidden in the queue. The 2 known-wrong similarity hints (0.909 wrong beat, 1.000 non-unique) were re-adjudicated, not inherited - skeptic confirmed each now points at its own source book.",
      "commit": "pending",
      "completed_by": "main-agent manual repair after the engine blocked at B1; verified by a 2-model hostile skeptic panel and a judge consult. The engine itself completed NO step.",
      "engine_outcome": "SKIPPED, never attempted: 'dependency not completed' (B1 blocked)."
    },
    {
      "id": "B3",
      "name": "BOTH goldens + grade baseline green against the live dev graph, floors restated per D6 \u2014 one step, so the engine's first phase gate (which hard-runs _test-golden after 3 steps) sees green. The step's own node proves \u00cele; PdV is proven by the immediately-following phase gate and grade by the close gate.",
      "status": "completed",
      "test_command": "make test-file FILE=\"tests/test_tour_golden_ile.py::test_ile_golden_overlap\"",
      "criterion_ids": [
        "AC-3"
      ],
      "files": [
        "fixtures/tour_golden/ile_oneway_90min.json",
        "fixtures/tour_golden/pdv_round_trip_60min.json",
        "tests/test_tour_golden_ile.py",
        "tests/test_tour_golden_pdv.py",
        "tests/test_tour_grade.py"
      ],
      "cost_class": "$0",
      "cost": "$0",
      "complexity": "high",
      "maxAttempts": 3,
      "attempts": 0,
      "depends_on": [
        "B2"
      ],
      "proof": "COMPLETED. make _test-golden GREEN for the first time (9 passed; RED at baseline on the fixture-provenance defect) and _test-grade 15 passed. Floors restated as ABSOLUTE hit counts per D6 from the first honest measurement: Ile 18/31=58.1% -> floor 15, PdV 4/21=19.0% -> floor 4 (the measurement itself). BOTH ABSOLUTE FLOORS WENT DOWN (Ile 20->15, PdV 5->4) and both files now say so plainly; an earlier comment claimed the PdV bar 'was never met and never could be' and a hostile review REFUTED that from the repo's own records (PdV 12/18=66.7% on 2026-06-13, 10/18=55.6% after the pace pin). The retracted claim is replaced by the true reason the numbers are not comparable: only 8 of the old fixture's 25 tag keys existed in the source document, so 12/18 was scored against a partly fabricated expectation. ALSO FIXED HERE: make golden-probe was broken (Error 1, no output) - it greped the overlap number out of pytest ASSERTION text, so it only ever worked while the gate was RED; both goldens now print a GOLDEN-OVERLAP marker unconditionally and the recipe reads that. And src/tour/grade.py had TWO fail-opens: it read the deleted expected_beat_ids key and scored a zero-match tour 1.00, and (found by hostile review two lines below the first repair) a null expected_spine_area matched a null result for a free 0.20 of the baseline. Both now refuse loudly. Mutations: each fix reverted -> RED.",
      "commit": "pending",
      "completed_by": "main-agent manual repair after the engine blocked at B1; verified by a 2-model hostile skeptic panel and a judge consult. The engine itself completed NO step.",
      "engine_outcome": "SKIPPED, never attempted: 'dependency not completed' (B1 blocked)."
    },
    {
      "id": "B4",
      "name": "C8 ruling recorded: cap 850 unchanged, unreachability pinned, standing-position unit ruling closes the open question",
      "status": "completed",
      "test_command": "make test-file FILE=\"tests/test_tour_quality_rubric.py::test_c8_cannot_fire_on_engine_output_and_the_unit_ruling_is_recorded\"",
      "criterion_ids": [
        "AC-4"
      ],
      "files": [
        "src/tour/quality_rubric.py",
        "src/tour/selection.py",
        "tests/test_tour_quality_rubric.py",
        "specs/2026-07-19-tour-quality-standard/01-standard.md"
      ],
      "cost_class": "$0",
      "cost": "$0",
      "complexity": "normal",
      "maxAttempts": 2,
      "attempts": 0,
      "depends_on": [
        "B3"
      ],
      "proof": "COMPLETED. C8 cap UNCHANGED at 850 (750 rejected on the standing measurement: the certified-good corpus carries stops at 757/761/808). New pinned test derives from the live constants that MAX_DWELL_AUDIO_SECONDS=270 at SPOKEN_WPM makes C8 UNREACHABLE on engine output, so a clean C8 is evidence about the dwell cap and never about quality. The standing-position unit ruling is recorded in the two former decision-forcer tests, closing the open C8-unit question: human positions max 367 words (all pass); the Ile declared-stop hits (Conciergerie 901, Notre-Dame 1595) are eye-prose spans over many positions and are expected. Zero rubric behaviour change.",
      "commit": "pending",
      "completed_by": "main-agent manual repair after the engine blocked at B1; verified by a 2-model hostile skeptic panel and a judge consult. The engine itself completed NO step.",
      "engine_outcome": "SKIPPED, never attempted: 'dependency not completed' (B1 blocked)."
    },
    {
      "id": "B5",
      "name": "C11 demoted to report-only stat; C9 recorded as by-design on eye-prose; stale docstrings corrected",
      "status": "completed",
      "test_command": "make test-file FILE=\"tests/test_tour_quality_rubric.py::test_c11_reports_a_stat_but_emits_no_finding\"",
      "criterion_ids": [
        "AC-5",
        "AC-6"
      ],
      "files": [
        "src/tour/quality_rubric.py",
        "src/tour/spatial_check.py",
        "tests/test_tour_quality_rubric.py",
        "specs/2026-07-19-tour-quality-standard/01-standard.md"
      ],
      "cost_class": "$0",
      "cost": "$0",
      "complexity": "normal",
      "maxAttempts": 2,
      "attempts": 0,
      "depends_on": [
        "B4"
      ],
      "proof": "COMPLETED. C11 demoted to a report-only stat (stats['year_density_outliers']), emitting ZERO findings - the only rubric behaviour change in this ledger. Measured basis: C11 fired on 100% of human-vetted references (5/25 PdV positions, 6/29 Ile) and 8-11% of machine tours, and the multiple needed to admit the humans (3.82/4.19) exceeds the machine max (3.11), so no threshold clears the humans and still bites. C9 kept EXACTLY as-is with its eye-prose firing recorded as by-design (it ranks the owner's gold 19.11 above a machine median of 23.2 and matches the online 12-18-word ear guidance). spatial_check.py's two stale docstrings claiming it is wired into score_tour were corrected; tests/test_spatial_check.py's un-wired guard stays green (47 passed).",
      "commit": "pending",
      "completed_by": "main-agent manual repair after the engine blocked at B1; verified by a 2-model hostile skeptic panel and a judge consult. The engine itself completed NO step.",
      "engine_outcome": "SKIPPED, never attempted: 'dependency not completed' (B1 blocked)."
    }
  ]
}
